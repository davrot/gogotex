This is a minimal frontend scaffold for Phase‑03 (auth flow).

Commands:
- npm install
- npm run dev
- npm run build

Environment variables are loaded from `.env` / `.env.local` and match `frontend/.env.example`.
